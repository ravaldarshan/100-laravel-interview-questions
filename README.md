# laravel-interview-questions

**What is the use of console.php, channels.php, web.php, and api.php?**
```
They define routes: `console.php` for Artisan, `channels.php` for broadcasting, `web.php` for browser routes, `api.php` for API routes.
```